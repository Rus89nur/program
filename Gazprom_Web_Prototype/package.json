{
  "name": "gazprom-web-prototype",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "serve": "python3 dev-server.py",
    "serve:8080": "python3 -m http.server 8080",
    "autostart:install": "bash ./scripts/install_autostart.sh",
    "autostart:uninstall": "bash ./scripts/uninstall_autostart.sh"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
